#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "lyapunov_exponents.h"

typedef struct{
  int * entier;
}new_type;

void main() {
  size_t i;
  size_t test;
  test = 0;
  i = 1;
  ++i;
  if (!test)
    printf("%i", i);
};
